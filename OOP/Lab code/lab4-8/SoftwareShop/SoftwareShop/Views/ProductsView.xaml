<Window x:Class="SoftwareShop.Views.ProductsView"

        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"

        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"

        xmlns:vm="clr-namespace:SoftwareShop.ViewModels"

        xmlns:v="clr-namespace:SoftwareShop.Views"

        xmlns:models="clr-namespace:SoftwareShop.Models"

                    Background="{DynamicResource WindowBackgroundBrush}"

            Foreground="{DynamicResource PrimaryTextBrush}"

        Title="Software Shop" Height="600" Width="900">

    <Window.Resources>

        <!-- Updated Search TextBox Style -->
        <Style x:Key="SearchTextBoxStyle" TargetType="TextBox">
            <Setter Property="Height" Value="30"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="Background" Value="#3A4B5D"/>
            <!-- Match sidebar background -->
            <Setter Property="BorderBrush" Value="#ddd"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Foreground" Value="White"/>
            <!-- Match sidebar text color -->
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}" 
                        BorderBrush="{TemplateBinding BorderBrush}" 
                        BorderThickness="{TemplateBinding BorderThickness}" 
                        CornerRadius="5">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Updated Search Button Style -->
        <Style x:Key="SearchButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#3A4B5D"/>
            <!-- Same as sidebar background -->
            <Setter Property="Foreground" Value="White"/>
            <!-- Consistent text color -->
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Width" Value="80"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#1976D2"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#1565C0"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#90CAF9"/>
                                <Setter Property="Foreground" Value="#E0E0E0"/>
                                <Setter Property="Cursor" Value="Arrow"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Window.DataContext>

        <vm:MainViewModel/>

    </Window.DataContext>

    <Grid>

        <Grid.ColumnDefinitions>

            <ColumnDefinition Width="200"/>

            <ColumnDefinition Width="*"/>

        </Grid.ColumnDefinitions>

        <!-- sidebar -->

        <Border Grid.Column="0" Background="#3A4B5D" CornerRadius="10" Margin="0">
            <StackPanel Margin="20">
                <Button
            Name="LangButton"
            Content="En"
            Width="25"
            HorizontalAlignment="Left"
            Background="#005BBB"
            Cursor="Hand"
            Foreground="White"
            BorderThickness="0" 
            Click="ChangeLang"/>

                <TextBlock Text="{DynamicResource UserInfoText}" FontWeight="Bold" FontSize="24" Margin="0,0,0,15" Foreground="White" TextAlignment="Center"/>

                <Border Height="2" Background="#B0BEC5" Margin="0,0,0,15"/>

                <TextBlock Text="{DynamicResource ProductsNameText}" FontSize="18" Margin="0,0,0,5" FontWeight="Bold" Foreground="#B0BEC5"/>
                <TextBlock Text="{Binding LoggedUser.Name}" FontSize="18" Margin="0,0,0,10" Foreground="White"/>

                <TextBlock Text="{DynamicResource ProductsEmailText}" FontSize="18" Margin="0,0,0,5" FontWeight="Bold" Foreground="#B0BEC5"/>
                <TextBlock Text="{Binding LoggedUser.Email}" FontSize="18" Margin="0,0,0,10" Foreground="White"/>

                <TextBlock Text="{DynamicResource ProductsRoleText}" FontSize="18" Margin="0,0,0,5" FontWeight="Bold" Foreground="#B0BEC5"/>
                <TextBlock x:Name="RoleDisplay" Text="{Binding LoggedUser.Role}" FontSize="18" Margin="0,0,0,10" Foreground="White"/>

                <Button Content="{DynamicResource ProductsExitButtonText}" Cursor="Hand" Command="{Binding UnloginCommand}" Margin="0,10,0,0" Background="#D32F2F" Foreground="White" BorderThickness="0" Width="120"/>
                <Button Content="View Account Data" Cursor="Hand" Command="{Binding ViewAcc}" Margin="0,5,0,0" Background="#FF9800" Foreground="White" BorderThickness="0" Width="120"/>
                <Button Name="AddNewButton" Content="{DynamicResource ProductsAddNewButtonText}" Margin="0,10,0,0" Cursor="Hand" Command="{Binding AddCommand}" Visibility="Hidden" Background="#8BC34A" Foreground="White" BorderThickness="0" Width="120"/>

                <Button Content="Theme Change" Width="120" Height="35" Click="OnToggleTheme" Background="#FBC02D" Foreground="#333333" Margin="0,10,0,0"/>

                <!-- Undo/redo -->

                <Button Command="{Binding UndoCommand}" Margin="0,20,0,0" Cursor="Hand">Undo</Button>
                <Button Command="{Binding RedoCommand}" Margin="0,20,0,0" Cursor="Hand">Redo</Button>

              
            </StackPanel>
        </Border>

        <Grid Grid.Column="1">

            <Grid.RowDefinitions>

                <RowDefinition Height="40"/>

                <RowDefinition Height="*"/>

            </Grid.RowDefinitions>

            <!-- searchbar -->

            <Border Grid.Row="0" Background="#f5f5f5" Padding="5">

                <DockPanel LastChildFill="False" VerticalAlignment="Center" HorizontalAlignment="Left" >
                    <v:UserControl1 Width="50px" Margin="10,0,100,0"/>
                    <TextBox 

                        Width="320" 

                        Style="{StaticResource SearchTextBoxStyle}"

                        Margin="0,0,10,0"

                        Text="{Binding SearchText,UpdateSourceTrigger=PropertyChanged}" 

                    x:Name="SearchTextBox"

                    VerticalContentAlignment="Center"

                    ToolTip="Введите для поиска продуктов" />

                    <Button Content="{DynamicResource ProductsSearchButtonText}" 

                        Style="{StaticResource SearchButtonStyle}"

                        Command="{Binding SearchCommand,UpdateSourceTrigger=PropertyChanged}" Margin="0,0,20,0" />
                    <v:MyFirstControl  IntValue="65" Direct="First_Direct" />
                    <v:MySecondControl Text="f" Tunneling="Second_Tunnel" Bubbling="Second_Bubble"/>

                </DockPanel>

            </Border>

            <!-- main content -->

            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Margin="5,10,10,10">

                <ItemsControl ItemsSource="{Binding Products}" Name="Items">

                    <ItemsControl.ItemsPanel>

                        <ItemsPanelTemplate>

                            <WrapPanel IsItemsHost="True" Orientation="Horizontal" />

                        </ItemsPanelTemplate>

                    </ItemsControl.ItemsPanel>

                    <ItemsControl.ItemTemplate>

                        <DataTemplate DataType="{x:Type models:Product}">

                            <Border Width="200" Height="370" Margin="10" Background="White" CornerRadius="8" Padding="10" Cursor="Hand"

                                BorderBrush="#ddd" BorderThickness="1" >

                                <StackPanel>

                                    <Image Source="{Binding ImagePath}" Height="150" Stretch="UniformToFill" Margin="0,0,0,10"/>

                                    <TextBlock  FontWeight="Bold" FontSize="16" Margin="0,0,0,5">

                                    <Run Text="{Binding Name}"></Run>

                                    </TextBlock>

                                    <TextBlock TextWrapping="Wrap" Margin="0,0,0,10" Height="60">

                                    <Run Text="{Binding Description}"></Run>

                                    </TextBlock>

                                    <TextBlock  Foreground="#2196F3" FontWeight="SemiBold">

                                    <Run Text="{DynamicResource ProductsPriceText}"/>

                                    <Run Text=": "/>

                                    <Run Text="{Binding Price}"/>

                                    <Run Text="{DynamicResource ProductsCurrencySign}"/>

                                    </TextBlock>

                                    <Button

    Content="{DynamicResource ProductsDeleteButton}"

    Cursor="Hand"

    Margin="0,10,0,0"

    Visibility="{Binding DataContext.ItemsDeleteVisibility, RelativeSource={RelativeSource AncestorType=Window}}"

    Command="{Binding DataContext.DeleteCommand,    RelativeSource={RelativeSource AncestorType=Window}}"

    CommandParameter="{Binding}" />

                                </StackPanel>

                            </Border>

                        </DataTemplate>

                    </ItemsControl.ItemTemplate>

                </ItemsControl>

            </ScrollViewer>

        </Grid>

    </Grid>

</Window>