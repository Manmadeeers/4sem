    <Window x:Class="SoftwareShop.Views.AddView"
            xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
            xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
            Background="{DynamicResource WindowBackgroundBrush}"
            Foreground="{DynamicResource PrimaryTextBrush}"
            Title="{DynamicResource AddWindowTitle}"
            Width="400" Height="400"
            ResizeMode="NoResize"
            WindowStartupLocation="CenterOwner">
        <Grid Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Row="0" Grid.Column="0" Text="{DynamicResource AddProductName}" VerticalAlignment="Center" Margin="0,5"/>
            <TextBox Grid.Row="0" Grid.Column="1" Margin="5" Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" />

            <TextBlock Grid.Row="1" Grid.Column="0" Text="{DynamicResource AddProductDescription}" VerticalAlignment="Top" Margin="0,5"/>
            <TextBox Grid.Row="1" Grid.Column="1" Margin="5" Height="100" AcceptsReturn="True" TextWrapping="Wrap"
                     Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}" />

            <TextBlock Grid.Row="2" Grid.Column="0" Text="{DynamicResource AddProductPrice}" VerticalAlignment="Center" Margin="0,5"/>
            <TextBox Grid.Row="2" Grid.Column="1" Margin="5" Text="{Binding Price, UpdateSourceTrigger=PropertyChanged}" />

            <TextBlock Grid.Row="3" Grid.Column="0" Text="{DynamicResource AddProductImagePath}" VerticalAlignment="Center" Margin="0,5"/>
            <TextBox Grid.Row="3" Grid.Column="1" Margin="5" Text="{Binding ImagePath, UpdateSourceTrigger=PropertyChanged}" />

            <StackPanel Grid.Row="4" Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="5">
                <Button Content="{DynamicResource AddProductSaveButton}" Width="100" Margin="5,0" Command="{Binding AddNewCommand}" IsDefault="True"/>
                <Button Content="{DynamicResource AddProductCancelButton}" Width="100" Margin="5,0" Command="{Binding CancelCommand}" IsCancel="True"/>
            </StackPanel>
        </Grid>
    </Window>
